<!DOCTYPE html>
<html lang="en">
<head>
<!-- GOOGLE ADS CONVERSION TRACKING — fires on thank you page load -->
<script async src="https://www.googletagmanager.com/gtag/js?id=AW-16607386615"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'AW-16607386615');
  gtag('event', 'conversion', { 'send_to': 'AW-16607386615/dU4FCLCiraIcEPe3gu89' });

  function gtagReportCallConversion(url) {
    var callback = function() {
      if (typeof url !== 'undefined') { window.location = url; }
    };
    gtag('event', 'conversion', {
      'send_to': 'AW-16607386615/dU4FCLCiraIcEPe3gu89',
      'event_callback': callback
    });
    return false;
  }
</script>

<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Thank You | Pro-Tech Roofing Contractor &amp; Assoc Inc.</title>
<meta name="robots" content="noindex, nofollow">

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow:wght@400;600;700;800&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<style>
/* ── BRAND COLORS — matches index.html exactly ── */
:root {
  --green-dark:  #0c3510;
  --green-mid:   #1a5c1a;
  --green-lite:  #2e8b2e;
  --gold:        #f5c800;
  --gold-lite:   #ffe066;
  --white:       #ffffff;
  --near-white:  #f4f6f4;
  --light-gray:  #eef2ee;
  --text-dark:   #1c1c1c;
  --text-mid:    #3d3d3d;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: 'Barlow', sans-serif;
  background: var(--green-dark);
  color: var(--white);
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 2rem 1.5rem;
  position: relative;
  overflow-x: hidden;
}

/* subtle diagonal texture matching site's green sections */
body::before {
  content: '';
  position: fixed;
  inset: 0;
  background: repeating-linear-gradient(
    -45deg,
    transparent,
    transparent 40px,
    rgba(245,200,0,0.03) 40px,
    rgba(245,200,0,0.03) 80px
  );
  pointer-events: none;
  z-index: 0;
}

/* ── CARD ────────────────────────────────────── */
.card {
  position: relative;
  z-index: 1;
  background: var(--green-mid);
  border-radius: 20px;
  padding: 3rem 2.5rem;
  max-width: 600px;
  width: 100%;
  border-top: 6px solid var(--gold);
  box-shadow: 0 24px 64px rgba(0,0,0,0.45);
  text-align: center;
  animation: slideUp 0.6s ease both;
}

@keyframes slideUp {
  from { opacity: 0; transform: translateY(40px); }
  to   { opacity: 1; transform: translateY(0); }
}

/* ── COMPANY NAME ────────────────────────────── */
.company-name {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 1.3rem;
  letter-spacing: 0.08em;
  color: var(--gold);
  margin-bottom: 0.25rem;
}
.license {
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.12em;
  color: rgba(255,255,255,0.55);
  margin-bottom: 2rem;
}

/* ── CHECK CIRCLE ────────────────────────────── */
.check-circle {
  width: 88px;
  height: 88px;
  border-radius: 50%;
  background: rgba(245,200,0,0.12);
  border: 3px solid var(--gold);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 2.4rem;
  color: var(--gold);
  margin: 0 auto 1.8rem;
  animation: pop 0.55s cubic-bezier(0.175, 0.885, 0.32, 1.275) 0.3s both;
}
@keyframes pop {
  0%   { transform: scale(0); opacity: 0; }
  80%  { transform: scale(1.12); }
  100% { transform: scale(1); opacity: 1; }
}

/* ── HEADING ─────────────────────────────────── */
h1 {
  font-family: 'Bebas Neue', sans-serif;
  font-size: clamp(2.8rem, 8vw, 4rem);
  color: var(--gold);
  letter-spacing: 0.05em;
  margin-bottom: 0.6rem;
  line-height: 1;
}

.subtitle {
  font-size: 1.08rem;
  color: rgba(255,255,255,0.88);
  line-height: 1.75;
  margin-bottom: 2rem;
}

.subtitle strong {
  color: var(--gold);
  font-weight: 800;
}

/* ── DIVIDER ─────────────────────────────────── */
.divider {
  border: none;
  border-top: 1px solid rgba(245,200,0,0.25);
  margin: 0 0 1.8rem;
}

/* ── INFO BOX ────────────────────────────────── */
.info-box {
  background: rgba(12,53,16,0.6);
  border: 1.5px solid rgba(245,200,0,0.3);
  border-radius: 12px;
  padding: 1.3rem 1.6rem;
  margin-bottom: 2rem;
  text-align: left;
}

.info-box .info-title {
  font-family: 'Bebas Neue', sans-serif;
  font-size: 1.15rem;
  color: var(--gold);
  letter-spacing: 0.05em;
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 0.6rem;
}

.info-box p {
  font-size: 0.95rem;
  color: rgba(255,255,255,0.85);
  line-height: 1.7;
}

/* ── STEPS ───────────────────────────────────── */
.steps {
  display: flex;
  flex-direction: column;
  gap: 0.85rem;
  margin-bottom: 2rem;
  text-align: left;
}

.step {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
}

.step-num {
  width: 32px;
  height: 32px;
  min-width: 32px;
  border-radius: 50%;
  background: var(--gold);
  color: var(--green-dark);
  font-family: 'Bebas Neue', sans-serif;
  font-size: 1rem;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 900;
  margin-top: 2px;
}

.step-text {
  font-size: 0.95rem;
  color: rgba(255,255,255,0.88);
  line-height: 1.6;
}

.step-text strong {
  color: var(--white);
  font-weight: 700;
}

/* ── CALL BUTTON ─────────────────────────────── */
.cta-call {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  background: var(--gold);
  color: var(--green-dark);
  font-family: 'Bebas Neue', sans-serif;
  font-size: 1.4rem;
  letter-spacing: 0.07em;
  padding: 16px 36px;
  border-radius: 100px;
  text-decoration: none;
  font-weight: 900;
  width: 100%;
  box-shadow: 0 4px 20px rgba(245,200,0,0.35);
  transition: background 0.2s, transform 0.2s, box-shadow 0.2s;
  margin-bottom: 1.2rem;
}
.cta-call:hover {
  background: var(--gold-lite);
  transform: translateY(-2px);
  box-shadow: 0 8px 28px rgba(245,200,0,0.45);
}

/* ── BACK LINK ───────────────────────────────── */
.back-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: rgba(255,255,255,0.55);
  font-size: 0.92rem;
  text-decoration: none;
  transition: color 0.2s;
  margin-bottom: 2rem;
}
.back-link:hover { color: var(--gold); }

/* ── BADGES ──────────────────────────────────── */
.badges {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 0.6rem;
  padding-top: 1.5rem;
  border-top: 1px solid rgba(245,200,0,0.2);
}

.badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: rgba(12,53,16,0.5);
  border: 1.5px solid rgba(245,200,0,0.35);
  border-radius: 100px;
  padding: 7px 16px;
  font-size: 0.82rem;
  font-weight: 700;
  color: rgba(255,255,255,0.85);
  letter-spacing: 0.02em;
}
</style>
</head>

<body>

<div class="card">

  <!-- COMPANY NAME + LICENSE -->
  <p class="company-name">Pro-Tech Roofing Contractor &amp; Assoc Inc.</p>
  <p class="license"><i class="fa-solid fa-certificate" style="margin-right:4px;color:var(--gold);"></i>LICENSE #CCC1329196</p>

  <!-- ANIMATED CHECK -->
  <div class="check-circle">
    <i class="fa-solid fa-check"></i>
  </div>

  <!-- HEADLINE -->
  <h1>Request Received!</h1>
  <p class="subtitle">
    Thank you for contacting Pro-Tech Roofing.<br>
    Our team will reach out to you <strong>within 1 business hour</strong> to schedule your free estimate.
  </p>

  <hr class="divider">

  <!-- WHAT HAPPENS NEXT -->
  <div class="info-box">
    <div class="info-title">
      <i class="fa-solid fa-list-check"></i> What Happens Next
    </div>
    <p>Here is what to expect after submitting your request:</p>
  </div>

  <div class="steps">
    <div class="step">
      <div class="step-num">1</div>
      <p class="step-text"><strong>We review your request</strong> — our team receives your information immediately and begins reviewing your roofing needs.</p>
    </div>
    <div class="step">
      <div class="step-num">2</div>
      <p class="step-text"><strong>We call you back</strong> — a roofing specialist will contact you at the number you provided within 1 business hour.</p>
    </div>
    <div class="step">
      <div class="step-num">3</div>
      <p class="step-text"><strong>Free on-site estimate</strong> — we schedule a convenient time to inspect your roof and provide a detailed written estimate at no charge.</p>
    </div>
  </div>

  <!-- CALL BUTTON -->
  <a href="tel:3059793688" class="cta-call"
     onclick="return gtagReportCallConversion('tel:3059793688');">
    <i class="fa-solid fa-phone-volume"></i> Need Faster Service? Call Now
  </a>

  <!-- BACK LINK -->
  <a href="https://protechroofing.github.io/" class="back-link">
    <i class="fa-solid fa-arrow-left"></i> Back to website
  </a>

  <!-- TRUST BADGES -->
  <div class="badges">
    <span class="badge"><i class="fa-solid fa-shield-halved" style="color:var(--gold);"></i> Licensed &amp; Insured</span>
    <span class="badge"><i class="fa-solid fa-star" style="color:var(--gold);"></i> 5-Star Rated</span>
    <span class="badge"><i class="fa-solid fa-trophy" style="color:var(--gold);"></i> 20+ Years Experience</span>
    <span class="badge"><i class="fa-regular fa-clock" style="color:var(--gold);"></i> Mon–Sat 7AM–6PM</span>
  </div>

</div>

</body>
</html>
